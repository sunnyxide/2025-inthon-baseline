# 연산 우선순위 처리 문제 분석

## 🔍 현재 코드 분석

### 1. 괄호 처리 ✅
- `_gen_precedence()` 함수: **올바르게 처리됨**
  - `(a+b)*c` → `val = (a + b) * c` ✅
  - `a*(b+c)` → `val = a * (b + c)` ✅

### 2. 교환법칙 ✅
- `_safe_augment_expression()` 함수: **올바르게 처리됨**
  - `a+b` → `b+a` (값 동일) ✅
  - `a*b` → `b*a` (값 동일) ✅

### 3. 결합법칙 ⚠️
- 현재는 **구현되지 않음** (주석으로만 남아있음)
- 예: `(a+b)+c` vs `a+(b+c)` (값은 동일하지만 표현이 다름)

### 4. 연산 우선순위 ❌ **문제 발견!**

#### 문제점
`_gen_base_calculation()` 함수에서 연산자를 순차적으로 처리하고 있어서, 곱셈/나눗셈이 덧셈/뺄셈보다 먼저 계산되어야 하는 우선순위가 반영되지 않습니다.

#### 현재 코드 (130-156줄):
```python
# Build expression respecting operator precedence
# Group * and // first, then + and -
expr = terms[0]
val = values[0]

for i, op in enumerate(ops):
    next_val = values[i + 1]
    if op == "+":
        expr = f"{expr}+{terms[i+1]}"
        val = val + next_val
    elif op == "-":
        # ...
    elif op == "*":
        expr = f"{expr}*{terms[i+1]}"
        val = val * next_val
    elif op == "//":
        # ...
```

#### 문제 예시:
만약 `ops = ["+", "*"]`, `terms = ["2", "3", "4"]`인 경우:
1. 첫 번째 반복: `expr = "2+3"`, `val = 5`
2. 두 번째 반복: `expr = "2+3*4"`, `val = 5 * 4 = 20`

하지만 실제 수학 규칙에 따르면:
- `2+3*4` = `2 + 12` = `14` (곱셈이 먼저)

**현재 코드는 잘못된 답(20)을 생성합니다!**

---

## 🔧 해결 방법

연산 우선순위를 올바르게 처리하려면:

1. **방법 1**: 연산자를 우선순위에 따라 그룹화하여 처리
   - 먼저 `*`, `//` 처리
   - 그 다음 `+`, `-` 처리

2. **방법 2**: 표현식을 파싱하여 올바른 순서로 계산
   - 중위 표기식을 파싱하여 계산 트리 생성
   - 계산 트리를 순회하여 올바른 순서로 계산

3. **방법 3**: 괄호를 자동으로 추가하여 우선순위 명시
   - 곱셈/나눗셈 부분에 괄호 추가
   - 예: `2+3*4` → `2+(3*4)`

---

## 📊 영향도 분석

### 영향받는 카테고리:
- `base_calculation`: **심각한 영향** (연산 우선순위가 중요한 카테고리)
- `precedence`: 영향 없음 (이미 괄호로 명시)
- `expression_consistency`: 영향 없음 (2항 연산만 사용)
- `relational`: 영향 없음 (단순 패턴)
- `single_number`: 영향 없음

### 예상 오류율:
- `base_calculation`에서 `*`, `//`와 `+`, `-`가 혼합된 경우: **100% 오답**

---

## ✅ 권장 수정 사항

`_gen_base_calculation()` 함수를 수정하여 연산 우선순위를 올바르게 처리해야 합니다.

